{
  "name": "@navios/commander-tui",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/commander-tui/src",
  "prefix": "commander-tui",
  "tags": [],
  "projectType": "library",
  "targets": {
    "check": {
      "executor": "nx:run-commands",
      "inputs": ["^projectSources", "projectSources", "{projectRoot}/tsconfig.json"],
      "options": {
        "command": ["tsc --noEmit"],
        "cwd": "packages/commander-tui"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "inputs": ["^projectSources", "project"],
      "options": {
        "command": "oxlint --fix",
        "cwd": "packages/commander-tui"
      }
    },
    "test:ci": {
      "executor": "nx:run-commands",
      "inputs": ["^projectSources", "project"],
      "options": {
        "command": "vitest run",
        "cwd": "packages/commander-tui"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["projectSources", "{projectRoot}/tsdown.config.mts"],
      "outputs": ["{projectRoot}/lib"],
      "dependsOn": ["check", "test:ci", "lint"],
      "options": {
        "command": "tsdown",
        "cwd": "packages/commander-tui"
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "yarn npm publish --access public",
        "cwd": "packages/commander-tui"
      }
    },
    "publish:next": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "yarn npm publish --access public --tag next",
        "cwd": "packages/commander-tui"
      }
    }
  }
}
